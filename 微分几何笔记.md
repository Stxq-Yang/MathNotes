# 微分几何笔记：代数基础篇
## 知识回顾  
(本文中，n维实数的通常拓扑记作$(\mathbb{R}^n,\tau_{\mathbb{R}^n})$，其上度量记作$d_{\mathbb{R}^n}$。另定义指标集$I_n=[1,n]\cap \mathbb{N}$)  
  
点集拓扑中，我们已经知道这样两个概念  
### 第二可数空间  
*一个空间$(X,\tau)$是**第二可数空间**，当且仅当* $\exists \mathcal{B}\text{是}X\text{的基},s.t.\, \operatorname{card}(\mathcal{B})\le \aleph_0$  
  
### T2分离性(Hausdorff)  
*一个空间$(X,\tau)$是**T2空间(Hausdorff空间)**，当且仅当* $\forall x,y \in X,\exists U\in \mathcal{N}(x),V\in \mathcal{N}(y),s.t. \, U\cap V = \varnothing$  
  
另外我们回顾一下$\mathbb{R}^n$上的全微分  
  
### 全微分  
*令$U\in \tau_{\mathbb{R}^n}$, $f:U \to \mathbb{R}^n$在$a\in U$点可微，当且仅当存在线性映射$L:\mathbb{R}^n\to \mathbb{R}^n$  
$s.t.\; \forall V\in \mathcal{N}(0),\exists W\in \mathcal{N}(0),s.t.\; \forall h\in W \setminus \{0\},\frac{d_{\mathbb{R}^n}(f(a+h),f(a)+L(h))}{d_{\mathbb{R}^n}(h,0)}\in V$ 此时$L$被称为$f$在$a$处的导数，记作映射$Df:a\mapsto L$。即$Df:U\to L(\mathbb{R}^n,\mathbb{R}^n)\cong \mathbb{R}^{(n^2)}$*  
  
$C^0(U,\mathbb{R}^n)=\{f:U\to \mathbb{R}^n\mid f\text{连续映射}\}$  
  
$C^1(U,\mathbb{R}^n)=\{f:U\to \mathbb{R}^n\mid \forall x\in U,f\text{在}x\text{处可导},Df\in C^0(U,\mathbb{R}^n)\}$  
  
$C^k(U,\mathbb{R}^n)=\{f\in C^1\mid Df\in C^{k-1}(U,\mathbb{R}^{(n^2)})\}$  
  
$C^{\infty}(U,\mathbb{R}^n)=\bigcap_{n\in \mathbb{N}} C^n $  

### Jacobian矩阵
*若$F:\mathbb{R}^n \to \mathbb{R}^m$有
$F:\mathbf{x}\mapsto \begin{bmatrix}
f^1(\mathbf{x})\\
f^2(\mathbf{x})\\
\cdots\\
f^m(\mathbf{x})
\end{bmatrix}$
则$\mathbf{F}$在$\mathbf{x}$处Jacobian矩阵满足*
$$
J_{ij}=\frac{\partial  f^i}{\partial x^j} (i\in[1,m]\cap \mathbb{N},j\in I_n)
$$


~~服了，我感觉要补习一下分析，分析少学了很多东西，全靠点集拓扑活下来的，虽然还能理解~~  
## *What is 流形?*  

微分几何无非是在研究一个不平的空间上的几何，我们首先需要一个能描述我们空间的结构，流形。

这是一个非常核心的概念，通俗的讲，流形是局部像欧氏空间的空间，形式的讲：  
  
### 流形  
*一个第二可数的T2空间$(M,\tau)$是$n$**维流形**，当且仅当*  
  
*$\forall p\in M,\exists U\in \mathcal{N}(p),\exists V\in \tau_{\mathbb{R}^n},s.t.\,\varphi:U \to V\text{是同胚映射}$，此时$(U,\varphi)$称为一个**坐标卡***  
  
第二可数意味着这个空间是可分的，不会出现特别大的结构让我们的空间没法局部化，Hausdorff性质意味着点不会全部粘在一起，这都非常自然，由此允许了度量的存在。坐标卡的存在，允许了围绕任意一点的附近区域都能展开到欧几里得空间下，精准的描述了局部的欧几里得性质，让每个点周围的区域都能像欧几里得空间一样处理，或者反过来说，我们用欧几里得空间进行同胚粘合出了一个新的空间。  
  
但是对于一个任意构造的空间。所有点分别构建起的坐标卡并不一定足够好，造成重叠区域出现冲突，让我们需要的那两个欧几里得空间上无法顺利的转换，或者转换的结果不好处理解析，因此我们需要选取一组”好“的坐标卡，来覆盖我们的空间  
### 图册  
*在流形$(M,\tau)$中，图册$\mathcal{A}$是一族坐标卡，记作$\mathcal{A}=\{(U_i,\varphi_i)\}_{i\in I}$，满足  
$\bigcup_{i\in I} U_i=M$*  
  
### 相容性  
*在流形$(M,\tau)$中，坐标卡$(U,\varphi),(V,\psi)$是$C^k$相容的,当且仅当$\psi \circ \varphi^{-1} \in C^k(\varphi(U\cap V),\mathbb{R}^n)$*  
  
*图册$\mathcal{A}$是$C^k$相容的,当且仅当$\forall (U,\varphi),(V,\psi)\in \mathcal{A},(U,\varphi)$和$(V,\psi)$是$C^k$相容的*  
  
我们固然希望一个完美的图册是$C^\infty$相容的，这样每一点的周围空间都能光滑的粘合起来，但是我们很容易“恶意“构造一个流形让这个图册不存在，或者不足以充分的我们的空间，所以我们不得不约束我们研究的范围  
### 光滑流形
*流形$(M,\tau)$是光滑流形，当且仅当其存在光滑图册*

这是一个非常完美的结构，我们可以在上面做任何微分操作。而且由于要求每个点周围都可以光滑的展开到欧几里得空间上，也可以的光滑的粘合起来，所以整个流形看起来也非常圆润。

由于光滑图册有可能有许多个，我们需要找到一个最大的，才易于我们研究

### 极大光滑图册
*在光滑流形$(M,\tau)$中，$\mathcal{A}$是其极大光滑图册，当且仅当$\mathcal{A}$是$C^\infty$相容，且 $\forall \text{坐标卡}(U,\varphi),(\mathcal{A}\cup \{(U,\varphi)\}$是$C^\infty$相容的 $)\Rightarrow ((U,\varphi)\in \mathcal{A})$*

## 切空间  
  
求导高中数学就学过，并且很简单对吧？~~如果导数就觉得难，那不会有这份笔记了~~。那么为什么那种一维的曲线问题要求导呢？就是为了捕捉曲线上某点的局部性质。比如说我们可以用导数的正负性来捕捉函数的单调性。本质上来讲，我们能用导数找到某一点的切线，然后在切线上找到一个有方向（正负性）的量，能很好的描述曲线的变化趋势。到了流形空间中，我们陷入了“找不到北”的困境，怎么办呢？我们也需要找到流形中每一点上能描述大小方向的对象。但是描述什么大小方向呢？当然是空间中任意经过这一点曲线在这里的变化量，或者说速度向量咯。那么我们就用一个泛函来捕捉它。

我们考虑化归欧氏空间曲线的导数。原本的导数用的是差商的形式，然而我们没有办法定义距离，更不要说差商了。我们甚至不知道$0$在哪里，因此我们不妨，也不得不再去找找导数的其他性质。为了表示保证计算出来的是一个向量，那么线性性我们是需要给到的。

不难想起链式法则。但是一方面，它不方便用常规的运算表示，而是需要引入复杂的复合操作。并且它对于任意阶导数也类似的能成立(Faà di Bruno公式)，所以它并不合适。

那么我们其实只有乘法法则了。来吧，我们继续想象那个欧氏空间的面积含义。我们想象一个空间，一根轴是拉平的我们的流形，另一根就是实数轴。现在放入一个$f(x)$和$g(x)$。它们乘出一个积$f(x)g(x)$，我们升一倍维度当它是个有向面积。那么这个有向面积的速度向量是什么样子的？很简单，$g$那个方向是$g'f$，$f$那个方向是$f'g$，然后，增量只是一个矢量和。得到$g'f+f'g$，乘法法则成立。

现在，我们的速度向量似乎是得到了，然而我们还需要再规范的表述一下

### 导子与切空间  
*在光滑流形$(M,\tau)$中，$p\in M$，线性算子$v:C^\infty(U,\mathbb{R})\to \mathbb{R}\; (U\in \mathcal{N}(p))$是$p$处的一个导子，当且仅当$\forall f,g \in C^\infty(U,\mathbb{R}^n),v(fg)=f(p) v(g)+g(p) v(f)$*  
  
*在光滑流形$(M,\tau)$中，$p\in M$, 点p的切空间$T_pM=\{v:C^\infty(U,\mathbb{R})\to \mathbb{R}\mid U\in \mathcal{N}(p),v$是$p$处的一个导子$\}$*  
  
导子就对微分算子的一个化归，但是确实管用，能捕捉我们想要的信息，具体怎么捕捉的，我们马上就能感受到。

### 切空间是线性空间吗？  
我们希望切空间能描述大小方向，那么我们就需要让它是线性空间。自然想到构造  
  
$(T_pM,\mathbb{R},+,\cdot)$其中  
  
1. $\forall v_1,v_2\in T_pM,(v_1+v_2)(f)=v_1(f)+v_2(f)$  
2. $\forall v\in T_pM,c\in \mathbb{R},(c\cdot v)(f)=c(v(f))$  
  
结合定义不难证明它满足线性空间的条件，所以切空间上存在着一个这样的线性空间

### 切空间的维度
在一条一维的曲线上，它的切线也是一维的，同理，这个线性空间的维度和流形一样。我们来证明一下

*$\operatorname{dim}\, M=\operatorname{dim}\, T_pM$*

**证明:**

*由线性空间维度的定义，我们需要构造切空间的一个基。我们化归的思考，如果在欧几里得空间上，我们点$p$的切空间是什么样的？实际上一个$p$处的一个向量就是把一个函数的这个速度向量表示了，如果我们要分到$n$个方向上，无非就是$\frac{\partial}{\partial x^i}\big |_{p}$这就是欧氏空间中切空间的基*

*现在，我们取一张包含$p$的坐标卡$(U,\varphi)$。由于$\varphi$同胚时,$\varphi'(x)=\varphi(x)-\vec{d},\vec{d}\in \mathbb{R}^n$也同胚，故可以令$\varphi(p)=0$。为了便于分解，我们设$\varphi=(x^1,x^2,\cdots,x^n)$*

*接下来我们能做的事情非常简单，我们直接把$f$复合一个$\varphi^{-1}$射进$\mathbb{R}^n$里，然后导一发再用$x$取$\varphi(p)$给它射回来 (~~Doge~~)。也就是$\frac{\partial}{\partial x^i}\big |_p\stackrel{\mathrm{def}}{=} \frac{\partial\; f\circ \varphi^{-1}}{\partial x^i}\big |_{\varphi(p)}$。于是我们可以得到一个基的集合$\{\frac{\partial}{\partial x^i}\big |_p\}_{i\in I_n}$*

*接下来我们确保这些基线性无关。不妨假设$c^1,c^2,\cdots,c^n\in \mathbb{R},s.t.\, \sum_{i\in I_n} c^i\frac{\partial}{\partial x^i}\big |_p=0$。即对于任意的$f$，都有$\sum_{i\in I_n} c^i\frac{\partial}{\partial x^i}\big |_p=0$*

*不难想到$\forall j\in I_n$,取$x^j$作用于每一个基，可以得到$\frac{\partial x^j}{\partial x^i}\big |_p=\begin{cases}1&(i=j)\\0&(i\ne j)\end{cases}$。假设此时$\sum_{i\in I_n} c^i\frac{\partial}{\partial x^i}\big |_p=0$，则$c^j=0$,即$\forall j\in I_n,c^j=0$，得到这些基线性无关*

*接下来要证明这些基能张成整个空间。即$\forall D\in T_pM,\exists a^1,a^2,\cdots,a^n\in \mathbb{R},s.t.\, D=\sum_{i\in I_n} a^i\frac{\partial}{\partial x^i}\big |_p$*

*怎么办呢？我们不妨先任取一个函数$f\in C^\infty(U,\mathbb{R})$研究。即证$D(f)=\sum_{i\in I_n} a^i\frac{\partial}{\partial x^i}\big |_p f=\sum_{i\in I_n} a^i\frac{\partial\; f\circ \varphi^{-1}}{\partial x^i}\big |_{\varphi(p)}$*

*我们思考的关键是想把$D(f)$表示为等号右边的形式，那么我们需要拆掉$f$，然后应用莱布尼茨法则。$f$并不好拆，但是$\hat{f}\stackrel{\mathrm{def}}{=}f\circ \varphi^{-1}$作为$\mathbb{R}^n$上的函数就能用上我们最基础的工具们了。*

*我们还是化归到一维的情况下思考。设$h:\mathbb{R}\to \mathbb{R}$是光滑函数，那么由微积分的基本定理，可以得到$\forall r\in\mathbb{R},\int_{[0,1]} \frac{d}{dx}h(rx) dx=h(r)-h(0)$即$f(r)=h(0)+\int_{[0,1]}\frac{d}{dx}h(rx) dx$。其中$\frac{d}{dx}h(rx)=\frac{dh(rx)}{drx}\frac{drx}{dx}=r\frac{dh}{dx}\big |_{rx}$于是得到$h(r)=h(0)+r\int_{[0,1]} \frac{dh}{dx} dx$。如果记性好点，那么一定能想起我们在泰勒展开的积分余项中证过这个部分。*

*现在回归高维，我们定$r=(r^1,\cdots,r^n)$,光滑函数$h:\mathbb{R}^n\to \mathbb{R}$。现在我们取一个变量$s$，就令它在$[0,1]$中吧。像刚才那样，我们可以得到$h(r)=h(0)+\int_{[0,1]} r^i \sum_{i\in I_n} \frac{\partial h}{\partial r^i}\big |_{sr} ds=h(0)+\sum_{i\in I_n} r^i \int_{[0,1]} \frac{\partial h}{\partial r^i}\big |_{sr} ds$*

*这种拆分有点远了，但是终于，我们能绕回来了。$\hat{f}(x)=\hat{f}(0)+\sum_{i\in I_n} x^i \int^1_0 \frac{\partial \hat{f}}{\partial x^i}\big |_{sx} ds$，还原到流形上，令$\varphi(p)=0,\varphi(q)=x$，那么得到$f(q)=f(p)+\sum_{i\in I_n}x^i(q)g_i(q)$其中$g_i(p)=\int_{[0,1]} \frac{\partial \hat{f}}{\partial x^i}\big |_{0} ds=\frac{\partial \hat{f}}{\partial x^i}\big |_0=\frac{\partial}{\partial x^i}\big |_p f$。这已经非常漂亮了，我们直接把它放入D里，由线性性$D(f)=D(f(p))+\sum_{I_n}D(x^iG_i)$，再应用莱布尼茨法则$D(x^iG_i)=D(x^i)g_i(p)+x^i(p)D(g_i)=D(x^i)g_i(p)$。最后得到$D(f)=\sum_{i\in I_n} D(x^i)g_i(p)=\sum_{i\in I_n} D(x^i) \frac{\partial}{\partial x^i}\big |_p f$,即$a^i=D(x^i)\in \mathbb{R}$。终于，结束了*

*所以,$\{\frac{\partial}{\partial x^i}\big |_p\}_{i\in I_n}$是$T_pM$的基，$dim T_pM=n=\operatorname{dim}\, M$*

<p align="right">☐</p>

## 切丛
我们一定会想把所有的切空间并起来，但是怕有点什么冲突，那么就这么办吧

*定义*
$$
TM=\{(p,v)|p\in M,v\in T_pM\}
$$

它很方便一个键值对的查找，故自然而然得到一个投影映射$\pi:TM\to M$,其中$(p,v)\mapsto p$。并且我们也易于证明其原像$\pi^{-1}(p)$能组成$T_pM$，对于任意一点$p$，这个$\pi^{-1}(p)$被称为$p$处的纤维丛。你很快就能发现其微妙的作用。

### 切丛也是光滑流形
我们来关注一个更加有趣的性质吧。切丛竟然也是一个光滑流形！

往这个方向想想，我们需要构造一个拓扑结构。这很难思考，但我们可以一步一步的来。

我们需要先利用流形$M$的坐标卡。先取一个坐标卡$(U,\varphi)$,不妨设$\varphi=(x^1,\cdots,x^n)$

我们在刚才的证明中已经知道$T_pM$有基$\{\frac{\partial}{\partial x^i}\big |_p\}_{i\in I_n}$。即有任意$v\in T_pM$可表示为$v=\sum_{i\in I_n}v^i \frac{\partial}{\partial x^i}\big |_p \; (v^i\in \mathbb{R})$

现在，给你一个邻域上纤维丛中的元素$(p,v)$，你一定会想捕捉点$p$的坐标以及$v$对应的$v^i$。于是我们定义一个函数$\widetilde{\varphi}:\pi^{-1}(U)\to \mathbb{R}^n\times \mathbb{R}^n$。

我们知道$\mathbb{R}^n\times \mathbb{R}^n\cong \mathbb{R}^{2n}$,可以赋予一个标准拓扑$\tau_{\mathbb{R}^{2n}}$。我们就干脆把它射回去，即走$\widetilde{\varphi}^{-1}:\mathbb{R}^{2n}\to \pi^{-1}(U)$，不难证明存在拓扑使得其为同胚映射，那么我们就得到了一个新的拓扑，嗯好，接下来我们要用这个局部的子空间拓扑制造全局的$TM$的拓扑。

直接粘合起来显然是不行的，这时候一个猜想呼之欲出，那就是用相容性，实际上有这样一个引理

*命题: 在光滑流形$M$中，若$(U,\varphi)$和$(V,\psi)$是$C^\infty$兼容的且$U\cap V\ne \varnothing$，则$\widetilde{\psi} \circ \widetilde{\varphi}^{-1} \in C^\infty(\widetilde{\varphi}(\pi^{-1}(U)\cap \pi^{-1}(V)),\mathbb{R}^n)$*

**证明:**

*不妨直接计算一下，我们得到$\widetilde{\varphi}^{-1}:(r^1,\cdots,r^n,v^1,\cdots,v^n)\mapsto(\varphi^{-1}(r^1,\cdots,r^n),\sum_{i\in I_n} v^i \frac{\partial}{\partial x^i} \big |_{\varphi^{-1}(r^1,\cdots,r^n)})$*

*那么下一步复合无非是代入$\widetilde{\psi}$，得到$\widetilde{\psi} \circ \widetilde{\varphi}^{-1}:(r^1,\cdots,r^n,v^1,\cdots,v^n)\mapsto (\psi(\varphi^{-1}(r^1,\cdots,r^n)),w^1,\cdots,w^n)$。$w$部分不太好处理，我们讨论一下。*

*$v=\sum_{i\in I_n}v^i \frac{\partial}{\partial x^i} \big |_{\varphi^{-1}(r^1,\cdots,r^n)}$这个东西，实际上应该写作$v(f)=\sum_{i\in I_n}v^i \frac{\partial f\circ \varphi^{-1}}{\partial x^i} \big |_{\varphi(\varphi^{-1}(r^1,\cdots,r^n))}=\sum_{i\in I_n}v^i \frac{\partial f\circ \varphi^{-1}}{\partial x^i} \big |_{(r^1,\cdots,r^n)}$吧。接着，我们只需要想办法把$\psi$换进去。由于$U\cap V$是公共邻域，对于任意一个光滑函数$g:U\cap V \to \mathbb{R}$直接处理$v(g\circ \psi^{-1}\circ \varphi)=\sum_{i\in I_n}v^i \frac{\partial g\circ \psi^{-1}}{\partial x^i} \big |_{(r^1,\cdots,r^n)}$正包含我们的新基$\frac{\partial}{\partial x^i} \big |_{\psi(\varphi^{-1}(r^1,\cdots,r^n))}$。就这样我们得到了$v'(g)=v(g\circ \psi^{-1}\circ \varphi)$*

*不妨定义为函子$F[v](g)=v(g\circ \psi^{-1} \circ \varphi)$（具体是什么函子学范畴论的时候再展开）。由于$g$可以是任意的$U\cap V$上的光滑函数，那么在这个范围内，$F[T_{\varphi^{-1}(r^1,\cdots,r^n)}M]=T_{\psi(\varphi^{-1}(r^1,\cdots,r^n))}M$，在之前我们已经证明其可以表示为$\sum_{i\in I_n} w^i\frac{\partial}{\partial x^i} \big |_{\psi(\varphi^{-1}(r^1,\cdots,r^n))},w^i\in \mathbb{R}$。故可以认为映射$(v^1,\cdots,v^n)\mapsto (w^1,\cdots,w^n)$同样是光滑的。*

*由于前面这个$\psi(\varphi^{-1})$也明确定义为光滑的，所以映射$\widetilde{\psi} \circ \widetilde{\varphi}^{-1}$也是光滑的*

<p align="right">☐</p>

有了这个命题，我们不仅仅得到了光滑图册内的拓扑兼容性，还直接证明了$\widetilde{\varphi}$可以是$TM$的坐标卡！加上与$\mathbb{R}^{2n}$的同胚关系，以及继承而来的第二可数性和Hausdorff性，可以确认$TM$是一个维度为$2\operatorname{dim}M$的流形！这个结论使得切空间拥有了非常优秀的拓扑性质。

>卜算子
>
>机房座渐稀，键止人声寂。构造忽得几分喜，碑落QED。
>
>欲笑惧回音，常事何足异？难记天琴蹄下弦，只证我思抵。

## 余切空间
如果说切空间和切向量是微商，那么我们也需要有微分，观察欧氏空间的情况,我们不难发现这两玩意就差了个$\Delta x$倍。那么我们干脆定义为对偶的空间
### 余切空间
*定义*
$$
T^*_pM=(T_pM)*=\{\alpha:T_pM \to \mathbb{R}\mid \alpha \text{是线性泛函}\}
$$

### 余切的线性结构
按照刚才的构造，我们就直接往切向量的基上乘一个$\Delta x^i$，那么自然而然得到这个$\Delta x^i$是余切空间的基。

具体的，我们定义为满足$dx^i |_p\frac{\partial}{\partial x^i}\big |_p=1$且$dx^i |_p\frac{\partial}{\partial x^j}\big |_p=0(i\ne j)$的$dx^i$为它的基，成立显然。

为了简记这种关系，我们定义克罗内克delta
$$
\delta^i_j=
\begin{cases}
1 &(i=j)\\
0 &(i\ne j)
\end{cases}
$$
于是
$$
\forall i,j \in I_n,dx^i|_p\frac{\partial}{\partial x^j}\big |_p=\delta^i_j
$$
即成立，$\{dx^i\big |_p\}_{i,j \in I_n}$为余切空间$T^*_pM$的基。

其实由此我们也可以看出为什么它叫余切
$$
\tan{(\alpha)}\cot{(\alpha)}=1
$$

## 余切丛
化归了那么多欧氏空间的性质，现在我们又要来化归自己了。想必任何人看到这个名词都已经能自然而然地写下这段定义了吧

*定义*
$$
T^*M=\{(p,v)|p\in M,v\in T^*_pM\}
$$

然后我们就要证明它也是个流形。还是一样的想法，对于相容坐标卡，确认$\widetilde{\psi} \circ \widetilde{\varphi}^{-1}$保证光滑。

同样设为$(r^1,\cdots,r^n,p^1,\cdots,p^n)\mapsto (s^1,\cdots,s^n,q^1,\cdots,q^n)$其中
$\alpha=\sum_{i\in I_n}p^i dx^i|_r$和$\beta=\sum_{I_n}q^i dx^i|_s$是相应的余切。

我们还是带一个$v=\sum_{I_n} v^i \frac{\partial}{\partial x^i} \big |_{(r^1,\cdots,r^n)}\in T_{(r^1,\cdots,r^n)}M$，那么通过$F[v]$我们可以将$v$从$T_{(r^1,\cdots,r^n)}M$转到$T_{(s^1,\cdots,s^n)}M$上，进而得到$\alpha(F[v])=\beta(v)$。我们搞一个$G[\alpha](v)=\alpha(F[v])$，那么和之前一样的逻辑，它能够覆盖相交处所有的切向量，然后$\beta=G[\alpha]$总能表示为$\sum_{I_n}q^i dx^i|_s$的形式，再加上$G[\alpha](v)=F[v]$仍然光滑，所以得到光滑。进而得到坐标卡和流形。
~~虽然没写命题，但写个qed吧~~

<p align="right">☐</p>

## 芽
### 层
我们可以用一个代数结构捕捉余切空间。我们不要流形了，就拿一个拓扑空间$(X,\tau)$说事。其实余切向量和切向量有个共同特征，首先他们都是函数，其次都是局部意义上的。那么我们干脆赋予拓扑空间一个这样的结构。首先，为了让函数可计算，我们拿一个环$(G,+,\cdot)$来。然后对于局部定义其上函数$\forall U\in \tau$, 环$(\mathcal{O}(U),+,\cdot)$满足$\forall f\in \mathcal{O}(U),f:U\to G$且有运算$(fg)(x)=f(x)g(x),(f+g)(x)=f(x)+g(x)$。这个$\{\mathcal{O}(U)\}_{U\in \tau}$就是一个**预层**。现在，问题来了，对于一个函数，现在在不同开集上分段分层后我们无法判断它们是否相等了，怎么办？老简单了只要交集的部分能完全重合就当他们一样就是了，记作
$$f\big |_{U\cap V}=g\big |_{U\cap V}$$
好！这就是**相容性条件**，没错，和坐标卡一样，也叫相容。

此外，特别的，对于任意包含关系 $V \subseteq U $，存在一个环同态 $ \rho_{UV}: \mathcal{O}(U) \to \mathcal{O}(V) $，称为限制映射，将函数限制到更小的开集上。

但是我们的函数不是全局的，那么我们怎么确定一个更大的函数呢？很好想，无非把相容的函数一段段粘起来，就像流形粘接局部的欧氏平面一样。拿一组开集拼定义域
$$\{U_i\}_{i\in I}$$
再来其上函数
$$\{f_i\mid f_i\in \mathcal{O}(U_i)\}_{i\in I}$$
相容，即重复段相同，这个思想和坐标卡真的很像
$$\forall i,j\in I,f_i\big |_{U_i\cap U_j}=f_j\big |_{U_i\cap U_j} $$
则可确定唯一的
$$f:\bigcup_{i\in I}U_i\to G$$
满足$\forall i\in I,\forall x\in U_i,f(x)=f_i(x)$
好，这就是**粘接公理**
满足**粘接公理**后，$\mathcal{O}(U)$就升级成了真正的**层**

### 函数芽
余切空间是定义在点附近的。现在给你一个点$p$，咋识别它周围的函数？利用邻域系$\mathcal{N}(p)$吧。我们关注一个极限在点周围是不是相同，其实只关心一个非常局部的行为，这种关心程度可以类比极限。当然，这里不只是极限相等，我们希望它们在某个邻域内完全相容。这个邻域和极限一样，形成了一个“滤子”。形式的将其定义为等价关系

对于$U,V\in \mathcal{N}(p)$,$f\in \mathcal{O}(U),g \in\mathcal{O}(V)$
$$
f\sim_p g\Longleftrightarrow \exist W\in \mathcal{N}(p),W\subseteq U\cap V, f\big |_W=g\big |_W
$$
现在等价类$[f]_p$就被称为一个**函数芽**。同大多数等价类一样，芽同样继承了环的计算。

现在将所有$p$处的函数芽集合起来，即
$$
\mathcal{O}_p=\{[f]_p\mid U\in \mathcal{N}(p),f\in \mathcal{O}(U)\}
$$
这就被称为**茎**

### 理想的构造
~~标题莫名诗意~~

我们还是回到余切空间的问题。余切向量是个线性函数，我们也要把函数线性化了。我们知道$f(x)$的最佳线性逼近是
$$
f(p)+f'(p)(x-p)
$$
而我们只要$f'(p)$，所以干什么？把$(x-p)=0$视作基础，提取$f'(p)(x-p)$的部分。故我们构建一个极大理想
$$
m_p=\{[f]_p\mid [f]_p \in \mathcal{O}_p,f(p)=0\}
$$
然后我们还需要干掉一个$f'(p)$继续为$0$的部分，或者说把它拉到$0$的位置。

然而这个语言不好表述。我们发现我们要商掉的正好就是$f'(p)(x-p)$为$0$的函数，那么定义
$$
m^2_p=m_p\cdot m_p= \{ \sum_{i\in I_n} [f_i]_p \cdot [g_i]_p \mid n \in \mathbb{N},\ [f_i]_p, [g_i]_p \in m_p \}
$$
不难证明它们是理想，同样继承了运算。

### 余切空间与切空间的新定义
如果我们定义层都取流形上的光滑函数$C^\infty(M,\mathbb{R})$，最终我们就能得到一个商环来表述余切空间
$$
T^*_pM=m_p/m^2_p
$$
我们用陪集定义余切向量，令微分算子
$$
df_p=[f]_p+m^2_p \quad ([f]_p \in \mathcal{O}_p)
$$
由于继续继承运算，线性则显而易见

另外有导子性，设$f=f_0+f(p)$,$g=g_0+g(p)\quad (f_0,g_0\in m_p)$ 
$$
fg=f(p)g(p)+f(p)g_0+g(p)f_0+f_0g_0
$$
同时取微分算子
常数项$f(p)g(p)$微没了，$f_0g_0\in m^2_p$被商掉了，剩下
$$
d(fg)_p=f(p)dg_p+g(p)df_p
$$
正好！

相应的切空间定义为对偶空间
$$
T_pM=(m_p/m^2_p)^*=\{v:m_p/m^2_p\to \mathbb{R}\mid v \text{是线性函数}\}
$$
同样有线性性。

这个语言虽然更复杂，但是却让我们更加清晰地刻画了切空间与余切空间的几何意义——线性逼近。

## 切映射与余切映射
来吧我们重新审视一下我们现在有的几个玩意。

1. $\mathbb{R}$ 标量集
2. $T_pM$ 切向量集
3. $T^*_pM$ 余切向量集

这几个量会随着我们选的$x^i$改变而改变吗？显然是不会的，改变的是它的分量而已，这就是向量最基础的性质。

我们来看一下我们的$F[v](g)=v(g\circ \psi^{-1} \circ \varphi)$和$G[\alpha](v)=\alpha(F[v])$在干什么

### 切映射（推前）

在相容坐标卡$(U,\varphi)$和$(V,\psi)$中,$F[v](g)$干了个这样的事，把$v$从坐标$(U,\varphi)$转到了$(V,\psi)$中，这就是一个坐标转换。

那么我们给一个更加极端的转化。现在在两个光滑流形$M$和$N$中，存在$F:M\to N$是光滑。定义
$F_{*p}:T_pM\to T_{F(p)}N$
来转换切向量。化归我们的$F[v]$函子，我们定义$(F_{*p}v)(g)=v(g\circ F)$

证明一下良定性

**证明:**

*(线性性) 显然，因为$v$线性，所以$F_{*p}v$也线性*

*(莱布尼茨法则) $(F_{*p})(gh)=v((gh)\circ F)=v((g\circ F)(h\circ F))=(g\circ F)(p) v(h\circ F)+(h\circ F)(p) v(g\circ F)=g(F(p))(F_{*p}v)(h)+h(F(p))(F_{*p}v)(g)$*

<p align="right">☐</p>


### 切映射的计算
当然我们不能只专注于结构性质了，我们看看它怎么计算。我们取$M$的坐标卡$(U,x^1,\cdots,x^m)$，$N$的$(V,y^1,\cdots,y^n)$。我们放一个$v=\sum_{i\in[1,m]\cap \mathbb{N}} v^i \frac{\partial}{\partial x^i} \big |_{p}$进去，那么对于坐标函数$y^i$,$(F_{*p}v)(y^j)=v(y^j\circ F)=\sum_{i\in [1,m]\cap \mathbb{N}} v^i \frac{\partial}{\partial x^i} \big |_{p}(y^j\circ F)$。若定义$F^j=y^j\circ F\circ x^{-1}$，则得到$(F_{*p}v)(y^j)=\sum_{i\in [1,m]\cap \mathbb{N}} v^i \frac{\partial F^j}{\partial x^i} \big |_{p}$。于是$F_{*p}v=\sum_{j\in I_n} (\sum_{i\in [1,m]\cap \mathbb{N}} v^i\frac{\partial F^j}{\partial x^i} \big |_{p}) \frac{\partial}{\partial y^j} \big |_{F(p)}$，用矩阵的形式，记作$(F_{*p}v)^j=v^i\frac{\partial F^j}{\partial x^i} \big |_{p}$，其实与欧氏空间中的Jacobian矩阵很像，可以视作$J$作用于列向量$(v^i)$上
### 余切映射（拉回）
既然处理了$F[\alpha]$，我们来化归一下$G[\alpha]$。

还是在两个光滑流形$M$和$N$中，存在$F:M\to N$是光滑。定义
$F^*_{p}:T^*_{F(p)}N\to T^*_{p}M$
来转换余切向量

我们继续化归$G[\alpha]$函子。定义$\forall \alpha\in T^*_{F(p)}N,v\in T_{p}M$，则$(F^*_{p}\alpha)(v)=\alpha(F_{*p}v)$。良定性由$F_{*p}$继承。

### 余切映射的计算
继续化归切映射的。设$\alpha=\sum_{j\in I_n} \alpha^j dx^j \big |_{F(p)}$

$(F^*_{p}\alpha)(\frac{\partial}{\partial x^i} \big |_{p})=\alpha(F_{*p}\frac{\partial}{\partial x^i} \big |_{p})=\alpha(\frac{\partial F^j}{\partial x^i} \big |_{p} \frac{\partial}{\partial y^j} \big |_{F(p)})=\sum_{j\in I_n} \sum_{i\in [1,m]\cap \mathbb{N}} \frac{\partial F^j}{\partial x^i} \big |_{p} \alpha_j$

故$F^*_{p}\alpha$写作矩阵的形式为
$(F^*_p\alpha)_i=\alpha_i\frac{\partial F^j}{\partial x^i} \big |_{p}$，也可以视作Jacobian矩阵转置作用在行向量$(\alpha_i)$上。

### 推前与回拉的性质
既然它与Jacobian矩阵一致，那么某些微分的性质它便能继承。

比如链式法则
$$
(G\circ F)_{*p}=G_{*F(p)}\circ F_{*p}
$$
$$
(G\circ F)^*_{p}=F^*_{p}\circ G_{*F(p)}
$$

还有一个这样的关于恒等映射的性质
$$
(id_{M})_{*p}=id_{T_pM}
$$
$$
(id_{M})^*_{p}=id_{T^*_pM}
$$
成立性显然。它深刻的展示了我们的我们的微分微商的直观和切映射余切映射的联系。

### 整体映射

$$
F_*:TM\to TN, (p,v)\mapsto (F(p),F_{*p}v)
$$
$$
F^*:TM\to TN, (F(p),\alpha)\mapsto (p,F^*_{p}\alpha)
$$

由于$F$以及其切和余切映射在任意点$p$都光滑，所以显然整体映射光滑。

## 张量

好了，我们引入一个复杂的问题，如果我们需要统计两个量呢？

首先来一个我们最熟悉的东西，两个切向量的距离。其实我们这里还不需要仔细研究，就搞一个有序对$(v,w)$满足$v,w\in T_pM$。我们可以说就是找到了这个东西。我们如果要转换坐标，有一个$F:M\to M$的双射作为坐标转换的函数，那么可以变成$(F_{*}v,F_{*}w)$即可。当然还要求它满足线性性。

想象一下流形中的应力，过点$p$有一个面，上受到一个力。我们如何描述它？不难想到面是可以用法向量表示的，它其实是面上一对向量的叉乘，当然这个叉乘我们现在还描述不了，但是无疑可以确定这是用两个余切向量生成的余切向量，那么就用余切向量$\omega$表示，然后力不用我多说它是个微分的量，用切向量$v$表示。那么这个量就记作$(v,\omega)$。

为了能够协调的改变坐标系以符合物理原理。我们需要对它做坐标代换。不难想到吧，定义坐标转换函数(双射)$F:M\to M$,自己映射自己是存在相应的$F^{-1}:M\to M$的，那么直接把$(v,\omega)$变为$(F_{*}v,(F^{-1})^{*}\omega)$似乎实现了完美转换。

如果问题再升级，变成了$r$个切向量(逆变)和$s$个余切向量(协变)的一组，然后保持坐标变换不变，这样的量称为**张量**

当然我们形式化的表述这种量。

### 多重线性映射观点
张量需要线性性，其实咱把切向量和余切向量配对（函数是特殊的配对），收纳到一个等势的空间$\mathbb{R}$里面，然后搞成一个泛函。或者理解为把一组对一组的函数对偶化了，变成到实数的线性泛函。总之
$$
T:\underbrace{T^*_pM\times \cdots \times T^*_pM}_{r}\times \underbrace{T_pM\times \cdots T_pM}_{s} \to \mathbb{R}
$$
并且对于每个变量都有线性性。
则$T$是$p$点处一个$(r,s)$-张量。$p$点处全体$(r,s)$-张量的集合记作$T^{(r,s)}_pM$

我们很容易得到这样的结论
$$
T^{(0,0)}_pM\cong \mathbb{R}
$$
$$
T^{(1,0)}_pM\cong T_pM
$$
$$
T^{(0,1)}_pM\cong T^*_pM
$$
~~这里一度犯糖搞反了误导全文，不知道有没有全部修正过来或者有没有过度修正，已经不重要了~~
### 张量积
给你一个$A\in T^{(r_1,s_1)}_pM,B\in T^{(r_2,s_2)}_pM$，现在我们想要利用类似于乘法的结构得到一个$A\otimes B\in T^{(r_1+r_2,s_1+s_2)}_pM$。自然想到
$$
(A\otimes B)(\alpha^1,\cdots,\alpha^{r_1+r_2},v_{1},\cdots,v_{s_1+s_2})=A(\alpha^1,\cdots \alpha^{r_1},v_1,\cdots,v_{s_1}) B(\alpha^{r_1+1},\cdots \alpha^{r_1+r_2},v_{s_1+1},\cdots,v_{s_1+s_2},)
$$

这样的代数能不能再用抽象代数提炼一下呢？显然是可以的

### 分次代数

首先我们来看一下$T^{(r,0)}_pM$的积，那么$T^{(r_1,0)}_pM\otimes T^{(r_2,0)}_pM=T^{(r_1+r_2,0)}_pM$。其实这样的结构并不少见，比如说给一个$t$元$r$次单项式
$$
ax_1^{r_1}x_2^{r_2}\cdots x_t^{r_t},\sum_{i\in \{1,2,\cdots,t\}} r_i=r
$$
类似的(0,s)-张量也是这样一个东西对吧，无非是我们的$x_i$取了切向量并且没有办法交换了而已（也因此不便于用指数合并重复元素）。砍掉交换性是抽象代数里很基本的操作。因此我们用公理化语言描述一下分次代数这样的结构。

它有一个可以做加法的指数结构，和一个可以做“乘法”且可以按次数分类的基底。由于我们需要覆盖任意维度的张量，此时我们也会想到分次的单项式能生成多项式，类比用分维度的向量生成更高维度向量的直和运算，我们定义这样一个结构。

**(分次环)**

*设*$(\Gamma,+)$*是一个幺半群*，$(R,\oplus,\cdot)$*是一个环，则*$R$*是*$\Gamma$*-分次环当且仅当*

1. **(可分次)**

$$
R=\bigoplus_{\gamma \in \Gamma} R_\gamma
$$

2. **(乘法兼容分次)**

$$
R_\gamma \cdot R_\delta=R_{\gamma+\delta}
$$

为了提炼我们的张量和多项式都保持的可爱的线性性，我们定义

*补一下$k$-代数的定义，~~抽象代数没学好~~。设$k$是一个域，环$(A,+,\cdot)$是个$k$-代数当且仅当 $\cdot:k\times A\to A$构成一个作用且$(A,k,+,\cdot)$构成线性空间*。(这其实是一个可以成环的向量空间，或者理解为一个满足线性性的$k$作用在环上)

进一步，如果一个$\Gamma$-分次环还是一个$k$-代数，则称为$\Gamma$-分次$k$-代数。

我们很容易发现张量$(T^{(r,0)}_pM,\oplus,\otimes)$构成一个$\mathbb{N}$-分次$\mathbb{R}$-代数，其实$(T^{(0,s)}_pM,\oplus,\otimes)$也是。甚至于更进一步我们发现$(T^{(r,s)}_pM,\oplus,\otimes)$是一个$\mathbb{N}^2$-分次$\mathbb{R}$-代数！这就是一个很代数的观点，我们可以由此定义张量的结构。

### 张量的分量表示
回到前面坐标转换的问题。如果有一个坐标卡拆成$(U,(x^1,\cdots,x^n))$。我们希望把张量拆成分量，怎么办？

一定记得这两个东西：$\frac{\partial}{\partial x^i} \big |_{p}$和$dx^i$。这是切向量和余切向量的基。

如果我们要分解一个$(r,s)$-张量，我们为第$k$个逆变参数留方向$i_k$,第$t$个协变参数留方向$j_t$，就不妨处理基为$T(\frac{\partial}{\partial x^{i_1}} \big |_{p},\cdots,\frac{\partial}{\partial x^{i_r}} \big |_{p},dx^{j_1}\big |_{p},\cdots,dx^{j_s}\big |_{p})$，我们记分量值$T^{i_1 i_2\cdots i_r}_{j_1 j_2\cdots j_s}\in \mathbb{R}$

我们很容易得到
$$
T=\sum_{(i_1,\cdots,i_r)\in I_n^r} \sum_{(j_1,\cdots,j_s)\in I_n^s} T^{i_1\cdots i_r}_{j_1,\cdots,j_s} \otimes \frac{\partial}{\partial x^{i_1}} \big |_{p}\otimes \cdots \otimes \frac{\partial}{\partial x^{i_r}} \big |_{p}\otimes dx^{j_1}\big |_{p}\otimes \cdots\otimes dx^{j_s}\big |_{p}
$$

### 张量的坐标变换
好了，我们的未竞事业，坐标变换在拆开张量后变得容易了。给定相容坐标卡$(U,\varphi)$和$(V,\psi)$，令$\varphi=(x^1,\cdots,x^n),\psi=(y^1,\cdots,y^n)$。$F:M\to M$其实就是$id_M$映射，这个推前很朴素$F^j=y^j\circ \varphi^{-1}$。那么这个推前计算直接是乘$\frac{\partial F^j}{\partial x^i} \big |_{p}$。拉回呢？分量满足$\frac{\partial F^j}{\partial x^i} \big |_{p} dy^j\big |_{p}=dx^i\big |_{p}$，由于对偶直接转为$\frac{\partial x^i}{\partial F^j} \big |_{p} dx^i \big |_{p}=dy^i |_{p}$

因此，我们得到，张量变换中
$$T=\sum_{(u_1,\cdots,u_r)\in I_n^r} \sum_{(v_1,\cdots,v_s)\in I_n^s} T^{u_1\cdots u_r}_{v_1,\cdots,v_s} \otimes \frac{\partial}{\partial x^{u_1}} \big |_{p}\otimes \cdots \otimes \frac{\partial}{\partial x^{u_r}} \big |_{p}\otimes  dx^{v_1}\big |_{p}\otimes \cdots\otimes dx^{v_s}\big |_{p}\\
=\sum_{(i_1,\cdots,i_r)\in I_n^r} \sum_{(j_1,\cdots,j_s)\in I_n^s} T^{i_1\cdots i_r}_{j_1,\cdots,j_s} \otimes \frac{\partial F^j}{\partial x^i} \big |_{p}\frac{\partial}{\partial x^{i_1}} \big |_{p}\otimes \cdots \otimes  \frac{\partial F^j}{\partial x^i} \big |_{p}\frac{\partial}{\partial x^{i_r}} \big |_{p}\otimes \frac{\partial x^i}{\partial F^j} \big |_{p}dx^{j_1}\big |_{p}\otimes \cdots\otimes \frac{\partial x^i}{\partial F^j} \big |_{p}dx^{j_s}\big |_{p}$$
利用张量的代数性质，可以得到
$T^{u_1\cdots u_r}_{v_1,\cdots,v_s}=T^{i_1\cdots i_r}_{j_1,\cdots,j_s}\frac{\partial F^j}{\partial x^i} \big |_{p}\cdots \frac{\partial F^j}{\partial x^i} \big |_{p} \frac{\partial x^i}{\partial F^j} \big |_{p} \cdots\frac{\partial x^i}{\partial F^j} \big |_{p}$

~~额，忍不住了挂个qed记号~~

<p align="right">☐</p>

### 张量丛
这是什么不用我多说吧，
$$
T^{(r,s)}M=\{(p,v)|p\in M,v\in T^{(r,s)}_pM\}
$$
至于是不是光滑流形，刚才不是都做过坐标转换了吗？这些变换本身光滑，是不是显然了。

这里顺便把$\pi^{(r,s)}:T^{(r,s)}M \to M$的投影定义了，和切丛的一样，它也是个光滑映射。

## 外代数
其实我们注意到$(2,0)$-张量，这种东西令人想起向量点乘，对吧？另外我们还有一个叉乘来着，它求什么的？法方向上放张成平行四边形的面积，是一个有向面积。

我们推广到张量世界。因为叉乘有$\mathbf{a} \times\mathbf{b}=-(\mathbf{b}\times \mathbf{a})$，而且还需要$\mathbf{a}\times k\mathbf{a}=0$。我们发现我们的有向面积也需要。

我们如何构造一个代数结构来保证这两个性质呢？我们令我们的运算叫$\wedge$。其实不难发现有向面积是两个一维合成一个二维，并且在计算上也类似于乘法，因此猜测它和$\otimes$是紧密关联的。我们想办法对$\otimes$做个调整得到它。

我们试试看让它实现$v\wedge v=0$。我们想到商环结构，是不是能达到某种调整到$0$的效果啊？因此我们构造一个理想 (令我们的张量代数是$T(V,V^*)$，参数分别是切空间与余切空间)
$$
I=\bigcup_{v\in T(V,V^*)} \{v\otimes v\otimes w\mid w\in T(V,V^*)\}\cup \{w\otimes v\otimes v\mid w \in T(V,V^*)\}
$$

这个理想很理想吧，我们直接把张量环除掉这个玩意，得到$T(V,V^*)/I$

我们在等价类上做个代数就满足
$$
(a+I)\wedge (b+I)=a\otimes b+I
$$

省略$+I$简记为$a\wedge b$

它满足反交换性吗？我们证明一下

**证明**
$$
(u+v)\wedge (u+v)=0
$$
$$
u\wedge u+u\wedge v+v\wedge u+v\wedge v=0
$$
$$
u\wedge v+v\wedge u=0
$$
$$
u\wedge v=-v\wedge u
$$

<p align="right">☐</p>

## 张量场
对于所有张量丛的集体，我们发现它和$T^{(r,s)}_pM$代数结构很像，其实我们希望它也是个$\mathbb{N}^2$-分次$\mathbb{R}$-代数，但是跨点乘法没有意义，怎么办呢？

我们不难想到把它变成一个$T^{(r,s)}_pM$，用什么呢？一个映射$p\mapsto T^{(r,s)}_pM$这样的截面。这个截面固然可以逐点做积实现$A\otimes B:p\mapsto T^{(r_1+r_2,s_1+s_2)}_pM$这种效果，得到我们可爱的分次代数。$p\mapsto T^{(r,s)}_pM$这样的截面被称为$(r,s)$**-张量场**

其实这样的场还是很多的，比如引力电磁力这些场力，都是一点对应一个切向量，即一个$(0,1)$-张量场。

### 张量场的分量表示
其实这是一件很自然的事，取一个坐标卡$(U,(x^i))$，分量函数$T^{i_1 i_2\cdots i_r}_{j_1 j_2\cdots j_s}:U\to \mathbb{R}$也是自然的，于是我们就可以用分量描述一个场了。

## 向量场和李代数
一个$(1,0)$-张量，就是一个切向量。它构成的场$(1,0)$-张量场，是物理学中最常见的一类场了。它常被称为向量场。

现在，取一个光滑向量场$X$，我们可以输入一个点$p$，得到$X(p)$是一个切向量。记得吗？切向量是个导子，还能再塞一个函数进去，我们就记作$X_pf$。为了方便操作，我们可以令$Xf:M\to \mathbb{R}$使得$p\mapsto X_pf$是个映射，它也显然能继承光滑性。

### 李括号
我们复合两个光滑向量场$X$和$Y$，因为$Yf$其实是可以给导子做输入的。那么我们就得到了一个$X(Yf)$这样的结构。我们令复合场为$XY$。

按理来说，复合场应该是个向量场。然而我们如果计算
$$
(XY)(fg)=(X(Y(fg)))=X(Y(f)g+fY(g))=X(Y(f))g+X(g)Y(f)+X(f)Y(g)+fX(Y(g))
$$
和我们希望得到的多了两项$X(g)Y(f)+X(f)Y(g)$，咋办？注意到这两项看起来很对称，那么就交换$X$和$Y$，即减掉用$YX$去消去中间项，得到
$$
(XY-YX)(fg)=f((XY-YX)(g))+((XY-YX)(f))g
$$
完美。因此我们定义运算$[X,Y]=XY-YX$为李括号，它可以继续继承$X$和$Y$的光滑性

### 李代数
当然，我们毕竟不是高中生~~并非不是~~，当然要捕捉结构性质。我们希望一个什么样的结构?很简单，展开后变成导子形式。也就是说$X(g)Y(f)+X(f)Y(g)=0$，即反对称性$[X,Y]=-[Y,X]$，这个性质我们的$XY-YX$也满足，如果考虑双线性性已有，可以处理成$[X,X]=0$，其实和我们在前面定义外代数时差不多。然而光有这个性质是不能确定下来的，那么还需要什么呢？二元似乎无法给出更多的信息了，故我们不妨处理一下三元$[X,[Y,Z]]=[X,YZ-ZY]=XYZ-XZY+YZX-ZYX$，你会发现这个结构里置换一下全消了！所以我们得到一个式子$[X,[Y,Z]]+[Y,[X,Z]]+[Z,[X,Y]]=0$。现在这个结构唯一确定了。因此我们定义

**(李代数)** 

*对于标量域$\mathbb{F}$，$M$上有李括号$[\cdot,\cdot]\in M$满足*

1. **(双线性性)** 
$$
[aX+bY,Z]=a[X,Z]+b[Y,Z]
[X,aY+bZ]=a[X,Y]+b[X,Z]
$$
2. **(幂零性)**
$$
[X,X]=0
$$
3. **(Jacobi恒等式)**
$$
[X,[Y,Z]]+[Y,[X,Z]]+[Z,[X,Y]]=0
$$

不难发现$\mathbb{F}$-代数上总有李代数满足$[X,Y]=XY-YX$

## 外微分
### 外代数的丛结构

我们继续想办法把结构搞成分次，比如外代数。其实不难发现$T(T_pM,T^*_pM)$中，$\wedge$是自然而然带分次的，毕竟来自于$\otimes$，因此它还是个$\mathbb{N}^2$-分次$\mathbb{R}$-代数。于是干脆把商环记作$\Lambda_pM$,这个分次记作$\Lambda^{(r,s)}_pM$

那么丛结构是
$$
\Lambda^{(r,s)}M=\{(p,v)\mid p\in M,v\in \Lambda^{(r,s)}_pM\}
$$
称为$(r,s)$**-外代数丛**。一样可以用截面分次，截面被称$(r,s)$**-外代数场**，它依旧是个光滑流形，有光滑的投影映射。

### 外微分
我们需要对有向面积做做积分，那么就取一类特殊的$(0,s)$-外代数，毕竟累积的是微分，累积个微商意义不大。那么这个东西$(0,s)$-外代数就记作$s$**-微分形式**$\in \Omega^s_pM$，相应的丛和场称为$s$**-微分形式丛**$\Omega^sM$和$s$**-微分形式场**。多一形式的微分就类似于朴素微分的升一阶，现在我们搞一个可以提升形式的算子$d$作用于一个微分形式场来生成新的微分形式场。

咋办？不难想到在上面逐点乘以一个$1$-形式场，也就是一个余切场，即记作
$$
d(X)=f_X\wedge X
$$
倘若$X$是个$s$-形式，则可设$g:M\to \mathbb{R}$
$$
X=f_1\wedge \cdots \wedge f_s \wedge g\\
d(X)=-f_1\wedge \cdots \wedge f_s \wedge f_{X}\wedge g
$$

为了确定，我们先解决一下$0$-形式的情况。$d(g)=f_g\wedge g$是一个余切场，我们希望它应该满足
$$
(dg\big |_p)(v)=v(g)\; (v\in T_pM)
$$

我们考虑分解出一个
$X=Y\wedge g$
$$
d(Y\wedge g)=f_X\wedge Y\wedge g=-Y\wedge (f_X\wedge g)
$$

此时我们需要拆$f_{Y\wedge g}$，怎么处理呢？

先拿出一个
$$
d(Y)=f_Y\wedge Y,d(g)=f_g g
$$

我们拿一个切向量$v$进来，处理
$$
f_Y(v),f_g(v),f_{Y\wedge g}(v)
$$

它们是什么意义？其实就是场沿着$v$方向某种“缩放比例”，是一个变化率。我们希望它们满足什么？$Y$和$g$的比例不要互相干扰吧，那么最朴素的做法就是令
$$
f_{Y\wedge g}=f_Y+f_g
$$
代回去试试
$$
d(Y\wedge g)=-Y\wedge (f_Y\wedge g+f_g\wedge g)=-Y\wedge f_Y\wedge g-Y\wedge f_g\wedge g=d(Y)\wedge g-Y\wedge d(g)
$$
这个形式很眼熟吧，有点像我们的导子，只有一个符号区别。为了公理化表示它，我们不妨递归的拆了$Y$并给$g$，得到对于$r$-微分形式$X$
$$
d(X\wedge Y)=d(X)\wedge Y + (-1)^{r}X\wedge d(Y)
$$
这被称为反导子性。

但是其实这样还不足以确定，看一下这个问题
$$
d(d(X))=d(f_X\wedge X)=f_{f_X\wedge X}\wedge f_X\wedge X=(f_{f_X}+f_X)\wedge f_X\wedge X
$$
$$
=f_{f_X}\wedge f_X\wedge X+f_X\wedge f_X\wedge X
$$
$$
=d(f_X)\wedge X=f_X\wedge d(X)
$$
其实处理不干净了，那么我们干净利落定义$f_{f_X}=f_X$即
$$
d(d(X))=0
$$
完全确定。

因此我们定义$d$算子满足

1. **(升次线性性)** $d:\Omega^sM\to \Omega^{s+1}M$线性
2. **(0-形式等同微分)** 设$f:M\to \mathbb{R}$
$$
(df\big |_p)(v)=v(f)\; (v\in T_pM)
$$
3. **(反导子性)** 
$$
\omega\in \Omega^sM,
d(\omega \wedge \eta)=d(\omega)\wedge \eta + (-1)^{s}\omega\wedge d(\eta)
$$
4. **(幂零性)**
$$
d^2=0
$$
### 缩并
能升形式，那么我们搞一个降一形式，这个就比较简单粗暴，拿个$V$把第一个参数填掉就是
$$
(\iota_V \omega)(V_2,\cdots,V_s)=\omega(V,V_2,\cdots,V_s)
$$
既然同是变形式，可以猜测它满足反导子性
$$
\omega\in \Omega^sM,
\iota_V (\omega \wedge \eta)=\iota_V (\omega)\wedge \eta + (-1)^{s}\omega\wedge \iota_V (\eta)
$$
为什么？我们需要记住外代数是张量积上的商环。我们其实需要验证的是其在张量代数上成立吗？很单纯，张量上没有反交换性和符号，但总是有
$$
\iota_V (T\otimes P)=\iota_V (T)\otimes P+T\otimes \iota_V (P)
$$
带入参数展开几乎显然。现在处理到微分形式上，就变成等价类中的问题，类比$d$的做法，我们可以处理
$$
\iota_V ([g]\otimes [X])=[\iota_V (g)\otimes X]+[g \otimes \iota_V (X)]=\iota_V ([g])\wedge [X]+[g] \wedge \iota_V ([X])
$$
$$
=\iota_V ([g])\wedge [X]-\iota_V ([X])\wedge [g] 
$$
后续递归处理即可，得到反导子性
<p align="right">☐</p>

## 李导数
有了微分我们可以看看导数。处理$(0,0)-$场的问题我们前面李代数那里写过$Xf$，没有问题。如果处理向量场咋办?很简单，把$F$场复合进去变成$[X,F]$，使得$F$随着$X$流动，也就是那个复合的意义，结束。

但是更复杂的场怎么办呢？我们其实要想办法把外微分及其相逆的缩并，再和李代数结合起来。外微分和缩并最独特的性质是什么？那就是反导子性吧。反导子建立在分次之上，因此我们需要一个分次且反导子的李代数……嗯，很困难但是可以

### 超代数
其实我们前面反导子性中，有一个关于$(-1)^s$的系数。咋办，这玩意其实只留一个奇偶性，很简单我们只需要一个$\mathbb{Z}/2\mathbb{Z}$-分次环，这个结构被称为**超代数**。我们用$deg(a)$表述$a$的次，为了方便操作，我们定义其上运算满足超交换性
$$
ab=(-1)^{\deg(a)\deg(b)}ba
$$

在环乘法定义该性质，那就是个**超交换环**
### 李超代数
我们结合超代数改改李代数。线性性就这样没啥好动的，幂零性是为了推出对称性，所以改成超对称性即可加一个符号，至于Ja恒等式，补一个符号变成
$$
(-1)^{\deg(X)\deg(Z)}[X,[Y,Z]]+(-1)^{\deg(Y)\deg(X)}[Y,[X,Z]]+(-1)^{\deg(Z)\deg(Y)}[Z,[X,Y]]=0
$$
移项化简可以得到
$$
[X,[Y,Z]]=[[X,Y],Z]+(-1)^{\deg(X)\deg(y)}[Y,[X,Z]]
$$
完成，于是定义

*对于标量域$\mathbb{F}$，$\mathbb{Z}/2\mathbb{Z}$-分次环$M$上有李超括号$[\cdot,\cdot]\in M$满足*

1. **(双线性性)** 
$$
[aX+bY,Z]=a[X,Z]+b[Y,Z]
[X,aY+bZ]=a[X,Y]+b[X,Z]
$$
2. **(超对称性)**
$$
[X,Y]=-(-1)^{\deg(X)\deg(Y)}[Y,X]
$$
3. **(超Jacobi恒等式)**
$$
[X,[Y,Z]]=[[X,Y],Z]+(-1)^{\deg(X)\deg(y)}[Y,[X,Z]]
$$

### Gerstenhaber代数
我们现在要把导子性质真正放进李超代数了。我们其实希望我的李括号能像对向量场一样用，即$[X,F]$来表示，那么我们的李超代数一样处理，我们的反导子性就这样工作
$$
[X,YZ]=[X,Y]Z+sgn Y[X,Z]
$$
至于$sgn$怎么处理，额有点麻烦，我们希望分次有
$$
\deg([X,Y])=\deg(X)+\deg(Y)+1
$$
因此我们其实处理$[X,Z]$的时候$X$贡献的次数被莫名其妙$+1$了，咋办？再$+1$给它干掉。因此变成
$$
[X,YZ]=[X,Y]Z+(-1)^{(\deg{(X)}+1)(\deg(Y))} Y[X,Z]
$$
这就是一个**Gerstenhaber代数**。
### 移位
我们想办法把张量放进这样的分次，咋整？其实很容易想到处理$(r,s)$-张量成$r+s+2\mathbb{Z}$次，这很基础，但是问题就是我们的兼容性。我们其实希望$[X,f]$是$1$次的切向量，然而$X$是$1$次，$f$是$0$次，放入G代数得到的是$0$次，那么我们干脆调整一位变成$r+s+1+2\mathbb{Z}$次，这就是一个移位，由此我们可以把张量代数收纳到G代数中
### 李导数
终于，我们走出来了，我们可以用这个分次上的G代数描述导数了。这就是个李导数，我们定义一个记号
$$
\mathcal{L}_X F=[X,F]
$$

它有这样几个性质

**与缩并交换性**:
$$
\mathcal{L}_X\circ \iota_Y=\iota_Y \circ \mathcal{L}_X
$$
**证明:**

要证即证
$$
[X,\iota_Y F]=\iota_Y[X,F]
$$
对于$(0,0)$-张量场成立性显然，故更高形式设$F=f\wedge G$然后利用反导子性分解一下，化简过程写在这里太难看了就不贴了，反正发现左边等于右边结束就是。
<p align="right">☐</p>

类似还可以证明

**与$d$交换性**:
$$
\mathcal{L}_X\circ d=d \circ \mathcal{L}_X
$$
证明方法类似就是，反正都只是计算问题~~抽象代数的运算规则算起来挺不顺手的，建议效仿初中生狠练计算~~
### 李导数的计算
我们处理一下怎么计算。

1. 对于$(0,0)$-张量场,$[X,f]=Xf$
2. 对于$(1,0)$-张量场,$[X,F]=XF-FX$
3. 对于$(0,1)$-张量场,怎么办？我们其实可以想想把$d$和$\iota_X$复合一下分别展开反导子形式，毕竟这样形式数能拉回来，然后和前面复合向量场一样遇到了一坨消不掉的东西。然后在发现这两玩意交换一下能给它消掉，正好满足Ger的反导子要求和变形式要求，~~展开过程略，写在这里太难看了~~.因此得到
$$
[X,F]=\iota_X(dF)+d(\iota_XF)
$$
如果我们给算子按照$\circ$定义一个李代数，也可以优雅地写作
$$
[X,F]=[\iota_X,d]F
$$
这就是著名的**Cartan魔法公式**

4. 对于更复杂的场，按照反导子性展开外积即可

## 联络
### 仿射联络（协变导数）
如果我们希望不同点上的张量能够计算，怎么办呢？在仿射几何中，就是要做个仿射变换，把向量从这个点平移到那个点上去。那么我们现在也需要一个变换，这个变换是旋转+平移的操作。然而我们希望它尽可能是平移的，旋转是空间导致的，那么怎么办？很简单啊，我们用一个向量场$X$描述平移，然后把操作的向量场$Y$放进来，让它沿着$X$作平移，这个操作结果就记作$\nabla_X Y$,称为**仿射联络**

然而如果我们用李导数移动一个向量，它会因为空间的弯曲发生我们不需要的旋转和偏移，导致什么结果呢？平移后平行四边形的边不能“相等”（当然，取决于我们相等的定义，目前还没有所以是人为的）。因此我们定义一个向量场$T(X,Y)$，来计算一个$[X,Y]-T(X,Y)$。好，我们再看一下另一组边怎么算，很简单，一边是在$X$的场里移动来$Y$，一个是在$Y$的场里移动来$X$，它们之间差$T(X,Y)$，得到等式
$$
[X,Y]=\nabla_X Y-\nabla_Y X-T(X,Y)
$$
其实这就相当于我们用挠率定义了平行的概念。记得非欧几何的历史吧，就是从重新定义平行开始的，这里也重演了这一步，只是方式有点抽象。
### 挠率
好吧。我们不得不研究一下挠率才能研究联络的性质。挠率吃两个向量场吐一个向量场，对偶化掉输出就是个$(1,2)$-张量。挠率具有什么性质呢？画一个平行四边形$ABCD$来想想。把$AB$平移到$CD$，如果另一边差了一截，那么把$CD$平移到$AB$差的一截其实就是大小相同方向相反的，用代数的话来表达就是反交换性
$$
T(X,Y)=-T(Y,X)
$$ 
然后，它的线性性是什么样的？对$\mathbb{R}$-线性是显然的，但我们野心更大。把纸拿起来，弯曲一下。其实，如果整个空间按照一个光滑函数做运动，那么差的这一段其实一样受到弯曲即可。因此，它是$C^\infty(M,\mathbb{R})$-线性的，即有
$$
T(f(X),Y)=f(T(X,Y))
$$
关于$Y$参数的线性由反交换性易证。
### 联络的性质
既然联络来自于李导数，那么我们就用李导数的性质看看它需要继承过来多少。首先是线性性。我们想一想为什么？因为它要可以像李导数一样的完成拖拽，这是运算的基础，没有压根没法放在一个等式里。因此有对于$a$和$b$实数有
$$
\nabla_{X}(aY+bZ)=a\nabla_{X}Y+b\nabla_{X}Z
$$
$$
\nabla_{aX+bY}Z=a\nabla_{X}Z+b\nabla_{Y}Z
$$

然后，它继承李导数作为导数的特点，毕竟一样要能移动向量，一样得到一个向量的变化量。因此定义
$$
\nabla_X(fY)=(f)(\nabla_X Y)+[X,f] Y
$$
接下来我们看一下，幂零能成立吗？不能，如果能的话则有
$$
[X,Y]=\nabla_X Y+\nabla_Y X+T(X,Y)
$$
$$
[Y,X]=\nabla_Y X+\nabla_X Y+T(Y,X)=\nabla_X Y+\nabla_Y X+T(Y,X)
$$
列等式
$$
[X,Y]=-[Y,X]
$$
最终解出
$$
T(X,Y)=2\nabla_X Y-[X,Y]
$$
我们的挠率被定死了，显然不是我们想要的

至于Jacobi恒等式性质，要求太苛刻了，同样会定死挠率，所以我们也不要，具体计算过程略。

现在我们的协变导数能被唯一确定了吗？其实结合$[X,Y]=\nabla_X Y+\nabla_Y X+T(X,Y)$是可以的，然而我们寻求更简洁的表达。

变形为
$$
T(X,Y)=\nabla_X Y-\nabla_Y X-[X,Y]
$$
咋整？记住$T(X,Y)$的$C^\infty(M,\mathbb{R})$-线性,我们处理
$$
T(fX,Y)=\nabla_{fX} Y-\nabla_Y (fX)-[fX,Y]=\nabla_{fX} Y-f\nabla_Y (X)-[X,f]Y-f[X,Y]
$$
$$
=\nabla_{fX} Y-f\nabla_Y (X)-[X,fY]
$$
非常想要把$f$给提出来，即定义
$$
\nabla_{fX+gY}Z=f\nabla_{X}Z+g\nabla_{Y}Z
$$
当然，如果带入第二个维度，受限于李代数的性质，并不易于提取，故仅有一边有$C^\infty(M,\mathbb{R})$-线性。


最终我们的仿射联络只有两个性质足以蕴含一切

**(双线性性)**
$$
\nabla_{fX+gY}Z=f\nabla_{X}Z+g\nabla_{Y}Z
$$
$$
\nabla_{X}(aY+bZ)=a\nabla_{X}Y+b\nabla_{X}Z
$$

**(导子性)**
$$
\nabla_X(fY)=(f)(\nabla_X Y)+[X,f] Y
$$

它蕴含了挠率的那点性质。其实可以由此将$Y$推广到任意张量场，都被称为**联络**

### 曲率
回到联络的Jacobi恒等式的成立性问题。Jacobi恒等式是什么形式？
$$
[X,[Y,Z]]+[Y,[X,Z]]+[Z,[X,Y]]=0
$$
可以推出对于向量场有
$$
\mathcal{L}_{X}\mathcal{L}_{Y}Z-\mathcal{L}_{Y}\mathcal{L}_{X}Z-\mathcal{L}_{[X,Y]}Z=0
$$
如果我们$[\cdot,\cdot]$变成$\nabla_X Y$呢
$$
\nabla_X \nabla_Y Z-\nabla_Y \nabla_X Z+\nabla_{[X,Y]} Z=?
$$
首先，大多数挠率下不等于$0$。那是多少，不知道，但是我们可以设它为
$$
\nabla_X \nabla_Y Z-\nabla_Y \nabla_X Z+\nabla_{[X,Y]} Z=R(X,Y)Z
$$
这里的$R(X,Y)Z$是一个$(3,1)$-张量，吃三个$(0,1)$吐一个$(0,1)$，输出端对偶一下，干净利落。

### 协变导数的计算
这是最无聊的一部分，~~说实话，我都懒得算~~。思想很简单，~~但是很烦人~~，不过我们逃不掉。仔细想想倒是，不能算的理论算什么东西，对吧。

来，取坐标卡，我要开算！设为$(U,x^1,\cdots,x^n)$

由双线性性，设$X=\sum_{i\in I_n} v^i\frac{\partial}{\partial x^i}\big |_{p}$，则$\nabla_X Y$可以分解为
$$
\sum_{i\in I_n}  \nabla_{v^i\frac{\partial}{\partial x^i}\big |_{p}} Y=\sum_{i\in I_n}  v^i\nabla_{\frac{\partial}{\partial x^i}\big |_{p}} Y
$$
还是，设$Y=\sum_{j\in I_n} w^j\frac{\partial}{\partial x^j}\big |_{p}$，则进一步可以分解为
$$
\sum_{i\in I_n} (v^i\sum_{j\in I_n} \nabla_{\frac{\partial}{\partial x^i}\big |_{p}} (w^j \frac{\partial}{\partial x^j}\big |_{p}))=\sum_{i\in I_n} (v^i\sum_{j\in I_n} (w^j \nabla_{\frac{\partial}{\partial x^i}\big |_{p}} (\frac{\partial}{\partial x^j}\big |_{p})+\frac{\partial w^j}{\partial x^i}\big |_{p}\frac{\partial}{\partial x^j}\big |_{p}))
$$
设符号(并非张量，因为依赖于坐标选择)
$$
\Gamma^{k}_{ij}\frac{\partial}{\partial x^k}=\nabla_{\frac{\partial}{\partial x^i}\big |_{p}}\frac{\partial}{\partial x^j}\big |_{p}
$$
化简得到
$$
\nabla_X Y=\sum_{k\in I_n} ((X(w^k)+\sum_{i\in I_N}\sum_{j\in I_n}v^iw^j\Gamma^k_{ij})\frac{\partial}{\partial x^k})
$$
按照分量形式记作
$$
\nabla_X Y^k=X(w^k)+\sum_{i\in I_N}\sum_{j\in I_n}v^iw^j\Gamma^k_{ij}
$$
可以算了 ~~太累了~~
### 挠率的计算

其实我们的平行四边形图像并非正确，但是无伤大雅，我们能够抓住其关键性质就行，形式化证明还得靠联络。现在还是来计算。~~继续算吧，我有什么办法~~

好的，这个简单很多了。处理
$$
T(X,Y)=\nabla_X Y-\nabla_Y X-[X,Y]
$$
一顿计算的得到分量为 ~~我算你**的，抄个公式罢~~
$$
T^k_{ij}=\Gamma^k_{ij}-\Gamma^k_{ji}
$$
~~长那么好看有什么用，计算过程烦炸了~~

### 曲率的计算
方法就是带入坐标基，计算过程略 ~~计算力超人的物理学家给了我们结果就是~~
$$
R^l_{ijk} = \frac{\partial \Gamma^l_{jk}}{\partial x^i} - \frac{\partial \Gamma^l_{ik}}{\partial x^j} + \sum_{p\in I_n} \Gamma^l_{ip} \Gamma^p_{jk} - \Gamma^l_{jp} \Gamma^p_{ik}
$$
~~这个长得也一般了，真希望拿个李代数之类的收纳一下看起来或还有几分姿色。~~

~~“人靠衣裳马靠鞍”，公式靠结构（bushi）~~

~~看得出我很不喜欢算就是了~~


## 活动标架法
嗯，~~嘉当一世~~ 埃利·嘉当也看不下去这变态的计算，因此继魔法公式后，又给我们送来了一个很有用的东西——活动标架法。

固定坐标卡分解计算量太大了，但是我们可以试着不要固定。我们关键的是$x^i$吗？不是，其实是$\frac{\partial}{\partial x^i}\big |_{p}$。那么我们干脆不要，我们为每个点定义一组基，也就是搞$n$的向量场，并且在每个点线性无关。这就是活动标架
$$
\{e_i(p)\}_{i\in I_n}
$$
相应有一个组余切基(余切架)
$$
{\theta^j}_{j\in I_n}
$$
满足
$$
\theta^i(e_j)=\delta^i_j
$$
现在，拿出一个场$X$。我们按照这个走走看看可以把$e_i$和$e_j$建立联系。一边处理联络
$$
\nabla_X e_i
$$
另一边，我们考虑把每个分量加进来
$$
\sum_{j\in I_n} e_j
$$
显然不够，因为$X$没用，但是张量类型两边都已经是$(1,0)$了，那么怎么办？我们乘一个$(0,0)$-形式上去。怎么构造$(0,0)$-张量呢？很简单，我们拿一个$(0,1)$，然后把$X$放进去，简单粗暴地得到等式
$$
\nabla_X e_i=\sum_{j\in I_n} \omega^j_i(X) e_j
$$
我们省略两边的$X$，变为
$$
\nabla e_i=\sum_{j\in I_n} \omega^j_i e_j
$$
其中$\omega^j_i$被称为**联络形式**

我们算一下协变导数,设$Y=\sum_{i\in I_n} Y^i e_i$
$$
\nabla_X Y= \nabla_X (\sum_{i\in I_n}Y^ie_i)=\sum_{i\in I_n}X (Y^ie_i)=\sum_{i\in I_n}((X Y^i)e_i+Y^i(\nabla_X e_i))=\sum_{i\in I_n}((X Y^i)e_i+Y^i(\sum_{j\in I_n} \omega^j_i(X) e_j))=\sum_{i\in I_n}((X Y^i)e_i)+\sum_{i\in I_n}(\sum_{j\in I_n} Y^i\omega^j_i(X) e_j)
$$
$$
=\sum_{i\in I_n}((X Y^i)e_i)+\sum_{i\in I_n} \sum_{j\in I_n}Y^j\omega^i_j(X) e_i=\sum_{i\in I_n}((X Y^i)e_i+\sum_{j\in I_n}Y^j\omega^i_j(X) e_i)=\sum_{i\in I_n}((X Y^i)+\sum_{j\in I_n}Y^j\omega^i_j(X))e_i
$$
### 挠率的结构表示
回到
$$
T(X,Y)=\nabla_X Y-\nabla_Y X-[X,Y]
$$
带入基
$$
T(e_j,e_k)=\nabla_{e_j} e_k-\nabla_{e_k} e_j-[e_j,e_k]
$$
$$
=\sum_{i\in I_n} \omega^i_k(e_j) e_i-\sum_{i\in I_n} \omega^i_j(e_k) e_i-[e_j,e_k]=\sum_{i\in I_n} (\omega^i_k(e_j)-\omega^i_j(e_k)) e_i-[e_j,e_k]
$$
特别处理一下$[e_j,e_k]$，由于切向量放不到$\omega$的式子里不好看，所以考虑对偶化。注意到$\theta^i$是$1$-微分形式，那么$d\theta^i$是$2$-微分形式，可以吃两个切向量，我们试一试
$$
d\theta^i(e_j,e_k)
$$
其实我们缺了个公式，这里给一下,自证不难 ~~但烦死了~~
$$
d\omega (X,Y)=X \omega(Y) - Y\omega(X) - \omega([X,Y]).
$$
证明思路是用魔法公式反推就行。回到原问题
$$
d\theta^i(e_j,e_k)=e_j\theta^i(e_k)-e_k(\theta^i(e_j))-\theta^i([e_j,e_k])
$$
由$\theta^i(e_j)=\delta^i_j$，得到
$$
d\theta^i(e_j,e_k)=e_j\delta^i_k-e_k\delta^i_j-\theta^i([e_j,e_k])
$$
由于$e_i(1)=0$
$$
d\theta^i(e_j,e_k)=-\theta^i([e_j,e_k])
$$
我们设
$$
[e_j, e_k] = \sum_m C^m_{jk} e_m
$$
则
$$
\theta^i([e_j, e_k]) = \theta^i\left( \sum_{m\in I_n} C^m_{jk} e_m \right) = \sum_{m\in I_n}  C^m_{jk} \theta^i(e_m) = \sum_{m\in I_n}  C^m_{jk} \delta^i_m= C^i_{jk}
=-d\theta^i(e_j,e_k)$$
故
$$
[e_j, e_k] = \sum_{m\in I_n} -d\theta^i(e_j,e_k) e_m
$$
我们对最初的式子对偶化，则分量
$$
\theta^i(T^i(e_j,e_k))=\omega^i_k(e_j)-\omega^i_j(e_k)+d\theta^i(e_j,e_k)
$$
由外积性质最终得到
$$
\theta^i(T^i(e_j,e_k))=(d\theta^i+\omega^i_k\wedge \theta^k)(e_j,e_k)
$$
我们写作一个分量的形式
$$
\Theta^i=d\theta^i+\omega^i_j\wedge \theta^j
$$
满足
$$
\Theta^i(X,Y)=\theta^i(T(X,Y))
$$
是一个$(1,2)$-张量。这就是第一结构方程
### 曲率的结构表示

$$
\nabla_X \nabla_Y Z-\nabla_Y \nabla_X Z+\nabla_{[X,Y]} Z=R(X,Y)Z
$$
带入基$e_j,e_k,e_l$,一顿算得到
$$
R(e_k, e_l) e_j = \sum_{i\in I_n} (e_k(\omega^i_j(e_l)) - e_l(\omega^i_j(e_k)) - \omega^i_j([e_k, e_l]) + \sum_{m\in I_n} (\omega^i_m(e_k) \omega^m_j(e_l) - \omega^i_m(e_l) \omega^m_j(e_k))) e_i
$$
对偶化可以得到
$$
\theta^i(R(e_k, e_l) e_j) = d\omega^i_j(e_k, e_l) + (\omega^i_m \wedge \omega^m_j)(e_k, e_l)
$$
令
$$
\Omega^{i}_{j}(X,Y)=\theta^i(R(X,Y)e_j)
$$
则
$$
\Omega^i_j = d\omega^i_j + \omega^i_k \wedge \omega^k_j
$$
其中$\Omega$是个$(1,3)$-张量。

这个公式就非常好看了，被称为第二结构方程

## 流形上的测度
### 密度场
来吧，我们要做积分了！最简单的问题。给你一组切向量$v_1,\cdots,v_n$,求它张成平行体的体积。

欧氏空间的经验告诉我们$n$个向量可以确定这个平行体了，然而我们怎么算体积，这就是积分的问题。

我们定义一个
$$\omega_p:(T_pM)^n\to \mathbb{R}$$
由于线性，它是一个$(n,0)$-张量，也就是$n$-微分形式的张量。因此，定义一个特殊的微分形式丛$\Omega^nM$被称为**行列式丛**

小平行四边形对我们来讲到底是什么？没错，是微元。那么为了方便做计算，我们就给到一个布满空间的行列式丛，也就是一个行列式场。

但是行列式计算出来是有符号量会互相抵消，然而我们计算体积并不总是希望。我们定义$(0,0)$-张量场定向场
$$sign:M\to \{-1,1\}$$
则定向场和行列式场的张量积就是个密度场。

### 密度层与实数层
如果我们的余切向量是用芽定义的，那么密度其实还是一组层，只是多了大概三四层抽象的样子。我们把这个层记作$\mathcal{D}$

那么我们的积分就是把密度射进实数。但是我们要把实数变成层。首先我们希望结果是个常值函数，其次我们希望它能在我们的$\{\mathcal(O)(U)\}_{U\in \tau_M}$中。那么我们构造另外的一个层
$$
\mathcal{R}(U)=\{f:U\to \mathbb{R}\mid f\text{是常值函数}\}
$$
$$
\underline{\mathbb{R}}_M=\{\mathcal{R}(U)\}_{U\in \tau_M}
$$
为了定义我们的积分让它有效，我们对$\mathcal{D}$再加一个小限制
$$
\mathcal{D}_c(U)=\{\rho \mid \rho\in \mathcal{D}(U),\operatorname{supp}(\rho)\text{是}U\text{中的紧致集}\}
$$
其中
$$
\operatorname{supp}(\rho)=\overline{\{x\mid \rho(x)\ne 0\}}
$$
如果脑子里只有何意味那就回去复习点集拓扑。直观解释一下，支集就意味着其外$\rho$为$0$，紧致意味着它可以被有限个开集覆盖，因为如果不能那么我们确实没法积分。


### 积分
然后，我们的积分就是一个线性映射！
$$
\int \mathcal{D}_c\to \underline{\mathbb{R}}_M
$$
即满足
$$\int_U \mathcal{D}_c(U)\to \underline{\mathbb{R}}(U)$$
是线性的

然而这个条件并不够。最重要的是我们希望它放进实数集和勒贝格积分结果一样。那么定义局部的微分同胚$\psi:U\to \mathbb{R}^n$ (未必是坐标卡)，利用拉回转到实数集满足
$$
\int_U \rho = \int _{\psi(U)} (\psi^{-1})^*\rho
$$
我们的积分就被确定了。实际上线性性也可以从这里推出，因此我们只要求

**定义** *积分为*
$$
\int \mathcal{D}_c\to \underline{\mathbb{R}}_M
$$
*满足*
$$
\int_U \rho = \int _{\psi(U)} (\psi^{-1})^*\rho
$$

实际上只有密度场是可以积分的，因此当我们积分别的东西时，有时候就要乘以微分变量$dx=dx^1\wedge \cdots \wedge dx^n$来转为密度场，或者取子流形上。这是经典微积分中常常忽略的细节

## 子流形
我们研究一个流形中的流形。一种方式是化归子空间拓扑之类的来构造，不过这样似乎不太方便，怎么办？其实如果$A\subseteq B$，则$id_A:A\to B$。类似的，我们也要求有一个包含映射，然而它的要求不只是$id$那么简单
$$
i:S\to M
$$
这就是个包含映射。我们再加约束。首先$S$得成流形没有问题。其次，$i$是光滑的单射。再进一步，记得推前拉回吗?对，我们希望切空间与余切空间也是一致的，即$i_*$单射。此时,那么$S$就是$M$的**浸入子流形**。当然它虽然局部完美,但是允许了$S$有很诡异的拓扑结构。

再进一步，我们带一个子空间拓扑的信息。即$S$上的拓扑是来自于$M$的子空间拓扑。这在拓扑学中被称为嵌入，在这里就是**嵌入子流形**。这里,$S$的拓扑结构就是极好的，也被称为**正则子流形**。

子流形$S$有独立的维度,但是它必然$\le \operatorname{dim} M $。利用反证法，假设$ \operatorname{dim} M<\operatorname{dim} S$，由包含映射和推前的性质推出切空间的低维度线性结构 ( 维度为$2\operatorname{dim} M$ ) 将包含高维度的结构 ( 维度为$2 \operatorname{dim} S$ )，这在线性代数上无法成立，得到矛盾。

然后，对于嵌入子流形，子空间拓扑的开集族按照交集继承，因此坐标卡也可以缩小定义域的继承下来。现在，我们选定一个原流形的坐标$(U,\varphi)$。由于坐标卡分解是可以“转”的，故结合线性代数可以证明必然存在一个分解$\varphi=(x^1,\cdots,x^n)$使得
$$\forall p\in U\cap S,\forall k\in I_{\operatorname{dim} M}\setminus I_{\operatorname{dim} S},x^k(p)=c^k$$

其中$c^k$为常数。

这是嵌入子流形的充要条件，被称为**切片条件**

## 带边流形
如何描述一个有边界的空间/子空间呢？或许我们需要从流形的定义开始处理。我们其实要求这玩意局部同胚于一个有边界的结构。我们干脆构造一个。拿出一个$\mathbb{R}^n$的局部结构出来，我们直接沿着$x^n=0$隔轴而治，轴及以上的部分$x_n\ge 0$直接拿出来子空间拓扑，定义为半空间$(\mathbb{H}^n,\tau_{\mathbb{H}^n})$。然后，我们爆改流形局部欧几里得性的定义，改为
$$\forall p\in M,\exists U\in \mathcal{N}(p),\exists V\in \tau_{\mathbb{H}^n},s.t.\,\varphi:U \to V\text{是同胚映射}$$

我们区分一下边界点，要求很简单，拿出拓扑学的边界概念，直接坐闭包$\setminus $内部，得到就是$x_n=0$的点构成的子空间，记作$\partial\mathbb{H}^n$，现在如果一个点$p$,对于任意包含它的坐标卡$(U,\varphi)$满足$\varphi(p)\in \partial\mathbb{H}^n$，则$p$是一个**边界点**，$M$中所有边界点的集合就是**边界**$\partial M$，需要注意其通常不同于拓扑意义上的边界。而除了边界外的点都被称为**内点**。

## Stokes定理
我处理一下带边子流形上的积分。咱知道边界比带边流形少一维，密度场也差一维。那么边界上做的积分和整个流形上的是什么关系呢？用外微分升一下维度，联系一下朴素微积分中的Stokes定理，设包含映射$i:\partial M \hookrightarrow M$，就能快速写出等式
$$
\int_M d\omega=\int_{\partial M}i^* \omega
$$

怎么证明？展开写太复杂，简单记录一下

**证明思路:**

*首先设等式两边为泛函*
$$
L(\omega)=\int_M d\omega,R(\omega) =\int_{\partial M}i^* \omega
$$

*局部化，取$x\in M$处的芽$[\omega]_p$，构一个把$x$映射到原点的同胚$\varphi：U\cong \mathbb{H}^n$，设$\widetilde{\omega}=(\varphi^{-1})^*\omega$问题化作*
$$
\int_{\mathbb{H}^n} d\widetilde{\omega}=\int_{\partial \mathbb{H}^n}i^* \widetilde{\omega}
$$

*进行放缩，拿放缩映射$s_t:\mathbb{H}^n\to\mathbb{H}^n,s_t:x\mapsto tx$其中$t\in (0,1]$*

*由于积分与拉回交换，可以得到$L(s^*_t \widetilde{\omega})=L(\widetilde{\omega}),R(s^*_t \widetilde{\omega})=R(\widetilde{\omega})$*

*最后，取$t\to 0$的极限，设$D=L-R$。易证在紧支集拓扑下连续，$s^*_t \widetilde{\omega}$的支集收缩到原点，最终得到$D=0$*~~此处略去至少一百字~~

*最后利用层论做粘合就行*
<p align="right">☐</p>

## De Rham上同调
回到微分形式的问题，记得这个$d^2=0$吗？现在我们可以把它再提炼出来。对于一个微分形式$\omega$，假设$d\omega=0$?诶，出事了，它未必是这种$d^2=0$的情况，即未必有$\omega=d\alpha$。那么怎么办呢？把$d\omega=0$的$\omega$放在集合里，这就是**闭形式**，把$\omega=d\alpha$放在一个集合里，这就是**恰当形式**

其实闭形式和恰当形式是同次的对象。如果让你把同次的闭形式收集起来，你会发现它们的处境和$m_p$有几分神似，没错，它其实是$d:\Omega^k(M)\to \Omega^{k+1}(M)$的核！而恰当形式必然是闭形式，并且由同次外微分的定义它们甚至构成一个正规子群！现在，我们可以把它们拉到$0$，即构造商群
$$H^k_{dR}=\operatorname{ker}(d:\Omega^k(M)\to \Omega^{k+1}(M))/d(\Omega^{k-1}(M))$$
这就是De Rham上同调群。这个群描述了方程$d\omega=0$的解集，也就能描述流形上某些特征的，会因外微分变成$0$的微分形式，从而捕捉了流形的整体特征。

## 胞腔，单纯形与奇异单形
诶，为什么引入代数几何呢？因为我们有dR上同调了，那么引入一点也没关系啦。

这个概念很简单，就当最简单的几何对象理解，我们零维的胞腔是一个点，一维的是一条开线段，二维是开圆盘……非常简单就能定义，我们要求$n$-胞腔同构于$\mathbb{R}^n$中的开球。

诶，为什么不带边呢？当然球体的边太特殊了不好操作，咋办？我们让一维作闭直线，两点确定，二维作三角形，三点确定，以此类推。利用一个嵌入高一维的小巧思，再用坐标轴在第一象限内截出图形，我们再考虑最基本的对象。$(0,1)$和$(1,0)$的连线，就有曲线方程$x+y=1$。单位的三角形其实也有$x+y+z=1$.以此类推，得到$n$-单纯形为

$$\Delta^n = \left\{ (t_0, t_1, \dots, t_n) \in \mathbb{R}^{n+1} \ \middle|\ \sum_{i\in I_n}t_i = 1, \ t_i \ge 0 \right\}$$

然而我们希望它能够像胞腔一样的可以捏得动，同时希望有界，咋办？拿一个拓扑空间$(X,\tau)$接收，然后我们用一个连续映射
$$
\sigma:\Delta^n\to X
$$
这里这个连续映射$\sigma$就正好既有$\Delta^n$的信息，又能在$X$中捏得动，完美，这就是$n$-**奇异单形**

## 链复形与同调
### 上同调
回到$d$算子上，我们可以画出一张这样的图
$$
0\xrightarrow{d}\Omega^0(M)\xrightarrow{d}\Omega^1(M)\xrightarrow{d}\cdots \xrightarrow{d} \Omega^n(M)\xrightarrow{d} 0
$$
其中$d\circ d=0$。

(因为鸽巢原理$>n$的形式会被反对称性抵消，因此最后得到$0$)

那么我们还能再抽象一点，一组群$C^i$和一组算子$\partial_i$,按照从小到大的顺序排

$$
0\rightarrow C^1 \xrightarrow{d_1} C^2\xrightarrow{d_2}\cdots
$$
要求它满足$d_k\circ d_{k+1}=0$。这就是**上链复形**

其上同样有上同调群$H^n=\operatorname{ker}(d_n)/d_{n-1}(C^{n-1})$

### 同调
另外考虑单纯形。注意到线的边缘是点，三角形的边缘是线……如果我们定义取边缘的操作为$\partial_i$，则同样有
$$
\cdots \rightarrow C_2\xrightarrow{\partial_2}C_1\xrightarrow{\partial_1}C_0\rightarrow 0
$$
其中边界的边界还是空的，所以$\partial_ {k-1}\circ \partial_{k}=0$。把$C_i$视作群，则得到一个**链复形**。

其同调群为
$$H_n=\operatorname{ker}(\partial_n)/\partial_{n+1}(C_{n+1})$$

### 奇异同调
如果我们把所有奇异复形$\sigma:\Delta^n\to X$作为基。然后，我们允许它有逆元和任意的加法，生成一个非常自由的阿贝尔群，记作$S^n(X)$，这就是**奇异链群**。然后我们定义边缘算子$\partial_n:S_n(X)\to S_{n-1}(X)$满足$\partial_{n-1}\circ\partial_{n}=0$，天然的构成链复形，进而得到奇异同调群。

### 奇异上同调
我们把奇异同调变成一个上同调，拿标量域$\mathbb{F}$来，变成线性函数$f:S_n(M)\to \mathbb{F}$，然后直接得到对偶空间$S^n(M,\mathbb{F})=(S_n(M))^*$

边缘算子也要反过来，令
$$(\delta_n f)(c)=f(\partial_{n+1}c)$$
其中$f\in S^n(M,\mathbb{F}),c\in S_n(M)$，边缘的边缘变零保持了，其构造思想前面咱也用过类似的，不赘述。进一步我们就能得到奇异上同调群。

## de Rham定理
我们重新审视一下积分，现在微分形式进dR上同调，那么流形就进奇异同调，正好凑一对能拼出一个子流形及其上密度。
$$
H^n_{dR}(M)\times S_p(M)\to \mathbb{R}
$$
使得
$$
\langle [\omega],[c]\rangle\mapsto \int_c\omega
$$

然后做的事情非常简单粗暴，把$H_p(M)$做对偶化，变成一个映射
$$
H^n_{dR}(M)\to S^n(M,\mathbb{R})
$$
然后，利用Stokes等等定理，发现这个映射是线性同构，环同构，自然同构……不管怎么说就是
$$
H^n_{dR}(M)\cong S^n(M,\mathbb{R})
$$
这就是**de Rham定理**。这是代数与几何的桥梁了。


# 微分几何笔记（黎曼几何篇）
